# Copy this file to .env and replace placeholder values.
# cp .env.example .env
# ./n8nite env check

# ====================================================================
# Minimum configuration (required by this docker-compose setup)
# Sources:
# - https://docs.n8n.io/hosting/installation/server-setups/docker-compose/
# - https://docs.n8n.io/hosting/configuration/environment-variables/
# ====================================================================

# Public n8n URL (used by n8n behind nginx reverse proxy)
N8N_HOST=automation.example.com
# http or https
N8N_PROTOCOL=https
WEBHOOK_URL=https://automation.example.com/

# Runtime defaults
NODE_ENV=production
# Prefer a valid IANA timezone (for example: Africa/Algiers, Europe/Paris, UTC)
GENERIC_TIMEZONE=UTC
# Required so main + worker can decrypt the same credentials.
# `./n8nite env init prod` replaces this automatically with a generated secret.
# If you copy this file manually instead, replace it with a long stable random value.
N8N_ENCRYPTION_KEY=change_me_with_a_long_random_secret

# Postgres bootstrap credentials
# `./n8nite env init prod` keeps the usernames below and generates fresh passwords.
POSTGRES_USER=n8n_admin
POSTGRES_PASSWORD=change_me_admin_password
POSTGRES_DB=n8n

# Postgres app credentials used by n8n
POSTGRES_NON_ROOT_USER=n8n_user
POSTGRES_NON_ROOT_PASSWORD=change_me_n8n_user_password

# ====================================================================
# n8n-specific optional/custom settings
# Docs for all variables:
# https://docs.n8n.io/hosting/configuration/environment-variables/
# ====================================================================

# API base URL for n8n UI (usually auto-detected; keep only if needed)
VUE_APP_URL_BASE_API=https://automation.example.com/api

# Override test/tunnel webhook URL (usually same as WEBHOOK_URL on server)
WEBHOOK_TUNNEL_URL=https://automation.example.com/

# Editor URL override (useful when host/proxy path differs)
# N8N_EDITOR_BASE_URL=https://automation.example.com/

# Encryption and security
# N8N_SECURE_COOKIE=true
# N8N_BLOCK_ENV_ACCESS_IN_NODE=true
# N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true

# Execution retention/timeouts
# EXECUTIONS_DATA_PRUNE=true
# EXECUTIONS_DATA_MAX_AGE=336
# EXECUTIONS_DATA_PRUNE_MAX_COUNT=10000
# EXECUTIONS_TIMEOUT=3600
# EXECUTIONS_TIMEOUT_MAX=7200

# Queue/worker tuning for this compose stack
# OFFLOAD_MANUAL_EXECUTIONS_TO_WORKERS=true
# QUEUE_BULL_REDIS_DB=0
# N8N_WORKER_CONCURRENCY=10

# Reverse-proxy realtime transport
# N8N_PUSH_BACKEND=websocket

# Optional for nginx/install-nginx-and-deploy.sh
# Used only if a new HTTPS certificate must be created
CERTBOT_EMAIL=admin@example.com
